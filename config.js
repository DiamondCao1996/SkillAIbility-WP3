/* SkillAIbility WP3 – shared configuration
 * Paste the Web App URL of your Google Apps Script (see README.md) between the quotes.
 * Leave empty to run in "export only" mode (Submit downloads a JSON file instead).
 */
const SUBMIT_URL = "";
